dist: xenial

language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - intltool
      - libglib2.0-dev
      - libgtk-3-dev
      - libgtk2.0-dev
      - libgcrypt20-dev
      - nettle-dev
      - linux-libc-dev
      - librsvg2-bin
      - at-spi2-core
      - libnautilus-extension-dev

env:
  - WITH_GTK=3.0
  - WITH_GTK=2.0

script:
  - ./autogen.sh
  - export DISTCHECK_CONFIGURE_FLAGS=$(echo --with-gtk=${WITH_GTK} --enable-{debug,glib-checksums,nettle,linux-crypto,nautilus} --disable-{dependency-tracking,blake2})
  - ./configure ${DISTCHECK_CONFIGURE_FLAGS}
  - make -j$(nproc) VERBOSE=1 DESTDIR=$(mktemp -d) distcheck
